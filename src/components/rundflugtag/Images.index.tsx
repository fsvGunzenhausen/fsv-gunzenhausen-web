import plakat from '../../assets/components/rundflugtag/plakat.jpg';

const imageStore = {
  // Image store with full and preview images}
  plakat: { full: plakat, preview: plakat }
};

export default imageStore;

